<span class="not-prose mx-0">
    <a href="#"><img src="{{ $user->picture_url }}" alt="{{ $user->name }}'s Avatar" class="inline-block object-cover w-5 h-5 rounded-full" /></a>
    <span>{{ $user->name }}</span>
</span>
